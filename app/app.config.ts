/**
 * Define la configuración global de la aplicación.
 * Este archivo se utiliza para configurar aspectos como el tema de la interfaz de usuario.
 */
export default defineAppConfig({
  ui: {
    colors: {
      primary: 'indigo',
      neutral: 'zinc',
    },
  },
})
